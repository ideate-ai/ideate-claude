id: WI-087
type: work_item
title: Dense Machine-Parseable Spec Format
status: obsolete
complexity: high
scope:
  - {path: plan/work-items.yaml, op: create}
  - {path: plan/notes/, op: create}
  - {path: scripts/validate-specs.sh, op: create}
  - {path: skills/plan/SKILL.md, op: modify}
  - {path: skills/execute/SKILL.md, op: modify}
  - {path: skills/review/SKILL.md, op: modify}
  - {path: skills/refine/SKILL.md, op: modify}
  - {path: skills/autopilot/SKILL.md, op: modify}
  - {path: scripts/migrate-to-optimized.sh, op: modify}
depends:
  - "076"
  - "077"
  - "078"
  - "084"
  - "085"
  - "086"
blocks:
  - "079"
  - "082"
criteria:
  - "All structured work item data (id, title, scope, dependencies, criteria, complexity) is stored in a single file: `plan/work-items.yaml`"
  - "Implementation notes (prose detail for workers) are stored separately in `plan/notes/{id}.md` — one file per item, loaded only by the worker executing that item"
  - "A CLI validation tool exists at `scripts/validate-specs.sh` (or `.py`) that performs DAG validation, scope overlap detection, coverage checks, and criteria linting (viewer tool is deferred — not MVP)"
  - The consolidated format is ≤50% of the token count of the equivalent per-file markdown for the structured fields (measured on a 15-item plan)
  - "`skills/plan/SKILL.md` Phase 7 writes work items in the new format"
  - "`skills/execute/SKILL.md` Phase 2 reads work items from the new format"
  - "`skills/review/SKILL.md` Phase 3.5 (manifest generation) reads from the new format — or is replaced entirely, since the consolidated file IS effectively a manifest"
  - "`skills/refine/SKILL.md` Phase 3 (line 60) reads work items from the new format (not just Phase 7h write path — the read path in Phase 3 must also be updated)"
  - "`skills/refine/SKILL.md` Phase 7h appends new items to the consolidated file and determines next available ID from `work-items.yaml` (not from globbing `plan/work-items/*.md`)"
  - "`skills/autopilot/SKILL.md` Phase 3 reads from the new format"
  - "076's review manifest generation in autopilot is updated to read from `work-items.yaml` instead of globbing `plan/work-items/*.md` (076's manifest code must work with the new format, not be preserved as-is against a deleted format)"
  - "All prior changes to skill files are preserved: metrics logging (084), worker self-check (086), convergence check parsing (077), conditional curator model (078)"
  - "Non-LLM tooling can parse `work-items.yaml` to: build the dependency DAG, detect cycles, compute topological sort, find file scope overlaps, validate 100% coverage, and generate execution strategy groups"
  - The migration tool (085) is extended to convert existing per-file markdown work items to the new format
module: null
domain: null
notes: |
  # Implementation Notes: 087
  
  ### Format example
  
  ```yaml
  items:
    076:
      title: autopilot review manifest integration
      complexity: low
      scope: [{path: skills/autopilot/SKILL.md, op: modify}]
      depends: []
      blocks: [080]
      criteria:
        - autopilot Phase 6b generates review-manifest.md before spawning reviewers
        - all 3 reviewer prompts reference manifest not plan/work-items/*.md
        - journal-keeper prompt references manifest as index
      notes: plan/notes/076.md
  ```
  
  Keys are human-readable. Scope ops: `modify`, `create`, `delete`. Criteria as plain strings (no checkbox syntax). Notes in separate files, loaded only by the executing worker.
  
  ### Validation tool (MVP)
  
  `scripts/validate-specs.sh` subcommands: `dag` (cycle detection), `overlap` (scope conflicts between concurrent items), `coverage` (union of scopes vs expected files), `groups` (topological sort for execution strategy), `lint` (flag vague criteria terms). Viewer/renderer deferred.
  
  Implementation: Python with `pyyaml`, or shell with `yq`.
  
  ### Skill changes
  
  - **Plan**: write to `work-items.yaml` + `plan/notes/{id}.md` instead of per-file markdown
  - **Execute**: single Read of `work-items.yaml` + per-worker Read of notes file (replaces N glob+read calls)
  - **Review**: `work-items.yaml` replaces manifest generation — the YAML already IS a manifest
  - **Refine**: Phase 3 read path updated to load from `work-items.yaml`. Phase 7h appends new entries and reads highest ID from YAML (not from globbing markdown files)
  
  ### Migration (extend 085)
  
  Parse existing `plan/work-items/*.md` files, extract structured fields to YAML, move implementation notes to `plan/notes/`. Move originals to `plan/work-items-legacy/`.
  
  ### Backward compatibility
  
  Skills check: if `work-items.yaml` exists, use it; otherwise fall back to `plan/work-items/*.md`.
resolution: "Dense spec format superseded by YAML work-items in .ideate/work-items/ with MCP artifact server."
cycle_created: 1
cycle_modified: null
content_hash: "672fea444de9f70b6433a36195d97d86da66235f32eb5a8533b2e5a982e820d6"
token_count: 1173
file_path: .ideate/work-items/WI-087.yaml
