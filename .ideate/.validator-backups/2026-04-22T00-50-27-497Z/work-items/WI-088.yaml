id: WI-088
type: work_item
title: README.md — add manifest.json, update migration section
status: done
complexity: low
scope:
  - path: README.md
    op: modify
depends: []
blocks: []
criteria:
  - The artifact directory structure diagram in README.md includes `manifest.json` as a direct child of `{artifact-dir}/`, positioned before `steering/`
  - The `/ideate:plan` "What it writes" listing includes `manifest.json`
  - The Migration section no longer references `scripts/migrate-to-domains.sh`; the section either describes the `archive/` + `domains/` layout as the current default (no migration needed for new projects) or is removed entirely
  - The "Validation and Migration Tools" subsection for `migrate-to-domains.sh` is removed
  - "The `scripts/` line in the Plugin structure diagram reads `scripts/         # Utility scripts (validate-specs.sh, migrate-to-optimized.sh)` — no mention of migrate-to-domains.sh"
module: null
domain: null
notes: |
  # Implementation Notes: 088 — README.md updates

  ## Artifact directory diagram

  Add `├── manifest.json` as the first child of `{artifact-dir}/`, before `├── steering/`. This matches the ordering in `specs/artifact-conventions.md`.

  ## /ideate:plan "What it writes" listing

  In the "Skill Reference" → "/ideate:plan" → "What it writes" section (or equivalent), add `manifest.json` to the list. Place it first, before `steering/guiding-principles.md`.

  ## Migration section

  The Migration section references `scripts/migrate-to-domains.sh` which is being deleted (WI-091). Replace the section with a brief note:

  > New projects automatically use the current `archive/` + `domains/` layout. No migration is needed for projects created with ideate v2+.

  Remove any subsections that are migration-specific (e.g., "If you are running inside an active Claude Code session", "After migration").

  ## Plugin structure diagram

  Update the `scripts/` comment to remove `migrate-to-domains.sh`. The line should reference only current scripts: `validate-specs.sh`, `migrate-to-optimized.sh`.

  ## Validation and Migration Tools subsection

  Remove any subsection or documentation entry for `migrate-to-domains.sh`. Keep `validate-specs.sh` and `migrate-to-optimized.sh`.
cycle_created: 1
cycle_modified: null
content_hash: 946396778f09c6978b7a54452fb2f0eeb4c6b693e05aef660ace3e91cba4ec15
token_count: 580
file_path: .ideate/work-items/WI-088.yaml
resolution: Implemented during v3 migration
