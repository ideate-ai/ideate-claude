id: WI-089
type: work_item
title: CLAUDE.md — add manifest.json to artifact structure diagram
status: done
complexity: low
scope:
  - path: CLAUDE.md
    op: modify
depends: []
blocks: []
criteria:
  - The artifact structure diagram in CLAUDE.md includes `manifest.json` as a direct child of `specs/`, positioned before `steering/`
  - "The `scripts/` line in the Plugin structure section reads `scripts/         # Utility scripts (validate-specs.sh, migrate-to-optimized.sh)` — no mention of migrate-to-domains.sh"
module: null
domain: null
notes: |
  # Implementation Notes: 089 — CLAUDE.md updates

  ## Artifact structure diagram

  The current diagram in CLAUDE.md does not include `manifest.json`. Add it as the first entry under `specs/`:

  ```
  specs/
  ├── manifest.json           # Schema version identifier
  ├── steering/               # Guiding principles, constraints, research, interviews
  ├── plan/                   # Architecture, modules, work items, execution strategy
  ├── journal.md
  ├── archive/
  │   ├── incremental/        # Per work-item reviews (written by execute)
  │   └── cycles/{NNN}/       # Capstone review suites (written by review)
  └── domains/
      ├── index.md            # Domain registry + current cycle number
      └── {name}/             # policies.md, decisions.md, questions.md per domain
  ```

  ## Plugin structure section

  Update the `scripts/` comment to remove `migrate-to-domains.sh` reference. Replace with `(validate-specs.sh, migrate-to-optimized.sh)` to reflect the current state.
cycle_created: 1
cycle_modified: null
content_hash: 097ed5a916e6cfefdbdaa56f1228222ca5f785ebbea38c73a6ef946cf9ee9cf1
token_count: 407
file_path: .ideate/work-items/WI-089.yaml
resolution: Implemented during v3 migration
