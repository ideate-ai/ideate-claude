id: WI-192
type: work_item
title: Fix recursive CTE cycle protection and ambiguous column in query.ts
status: done
complexity: small
scope:
  - path: mcp/artifact-server/src/tools/query.ts
    op: modify
depends: []
blocks: []
criteria:
  - Recursive CTE in graph traversal mode uses UNION (not UNION ALL) to prevent duplicate rows on cyclic edges
  - Column n.id is aliased as node_id in the CTE output SELECT
  - ORDER BY clause at line ~485 uses node_id (not bare id) to prevent ambiguous column errors
  - Existing filter mode (non-recursive) is unchanged
  - TypeScript builds clean (tsc --noEmit)
module: null
domain: null
notes: |
  # WI-192: Fix recursive CTE cycle protection and ambiguous column

  ## Changes

  Two surgical fixes in `mcp/artifact-server/src/tools/query.ts`:

  ### Fix 1: UNION ALL → UNION (Q-75)
  In the recursive CTE body (around line 427), change `UNION ALL` to `UNION` in both the outgoing and incoming recursive steps. This deduplicates visited nodes, preventing exponential row growth when traversing cyclic edges (e.g., mutual `references` between two nodes with `direction: "both"`).

  ### Fix 2: Alias n.id as node_id (Q-76)
  In the CTE output SELECT (line ~430): change `n.id` to `n.id AS node_id`.
  In the ORDER BY (line ~485): change `ORDER BY depth, id` to `ORDER BY depth, node_id`.
  In the RawRow type: change `id: string` to `node_id: string`.
  Update any code that reads `row.id` from traversal results to use `row.node_id`.

  The filter mode (non-recursive path) is unchanged — it does not use the CTE.
cycle_created: 1
cycle_modified: null
content_hash: 0aa071fe85a6119fe2551fdae41b21625dbaa57980a07703f9fd8a6ed9c0c15d
token_count: 429
file_path: .ideate/work-items/WI-192.yaml
resolution: Fixed UNION ALL to UNION in depth=1 both-directions path; aliased n.id as node_id in all graph traversal paths
