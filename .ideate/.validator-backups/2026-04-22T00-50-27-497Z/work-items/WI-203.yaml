id: WI-203
type: work_item
title: Execute skill v3 update — remove MCP fallbacks, fix paths
status: done
complexity: medium
scope:
  - path: skills/execute/SKILL.md
    op: modify
depends:
  - "201"
blocks: []
criteria:
  - All ideate MCP tool calls are direct — no availability checks, no manual fallbacks
  - If ideate MCP tools are not available, skill stops with error message
  - archive/incremental/ references changed to .ideate/cycles/{NNN}/findings/ per P-19
  - journal.md direct writes replaced with ideate_append_journal calls
  - No outpost references — generalized to "external MCP servers"
  - spawn_session availability check preserved (external tool)
module: null
domain: null
notes: |
  # WI-203: Execute Skill v3 Update

  ## Changes

  For each ideate MCP tool call in execute/SKILL.md:

  ### Pattern to remove
  ```
  **MCP availability check**: Look in your tool list for a tool whose name ends in `ideate_X`... If found: call it... If not found, [manual fallback]:
  [extensive manual logic]
  ```

  ### Pattern to replace with
  ```
  Call `ideate_X` with ({parameters}).

  If the ideate MCP artifact server is not available, stop and report: "The ideate MCP artifact server is required but not available. Verify .mcp.json configuration."
  ```

  ### Specific tool calls to update
  - Phase 2 (resume detection): `ideate_get_execution_status` — remove manual glob + file read fallback
  - Phase 6 (worker context): `ideate_get_work_item_context` — remove manual 8-step file read fallback
  - Phase 10 (journal): `ideate_append_journal` — remove manual file append fallback
  - Phase 11 (status): `ideate_get_project_status` — remove manual aggregation fallback

  ### Path fixes
  - `archive/incremental/NNN-{name}.md` → `.ideate/cycles/{NNN}/findings/F-{WI}-{SEQ}.yaml` (P-19)
  - `journal.md` direct writes → `ideate_append_journal` calls

  ### Outpost
  - Replace "outpost" → "external MCP servers"
  - Preserve `spawn_session` availability check (external tool)
cycle_created: 1
cycle_modified: null
content_hash: bb480722b5f573f781f266323a257e4c4e4626d3183cc853b246ec772037b2c3
token_count: 532
file_path: .ideate/work-items/WI-203.yaml
resolution: "Verified complete: execute skill calls MCP tools directly, no outpost references"
