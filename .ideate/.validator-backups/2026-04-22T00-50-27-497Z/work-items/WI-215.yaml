id: WI-215
type: work_item
title: Cycle reporting script
status: done
complexity: small
scope:
  - path: scripts/report-cycle.sh
    op: create
depends: []
blocks: []
criteria:
  - scripts/report-cycle.sh is executable (chmod +x)
  - Script requires python3 (checks and exits with error if not found)
  - Script accepts optional METRICS_FILE argument (same auto-discovery pattern as scripts/report.sh)
  - "Script produces markdown output with sections: Cycle-over-Cycle Quality Trends, Convergence Speed, First-Pass Acceptance Rate"
  - Quality Trends section shows per-cycle finding counts by severity with trend indicator (improving/stable/degrading) — same logic as report.sh section_quality_trends but promoted to primary output
  - Convergence Speed section shows autopilot cycle count per cycle (from convergence_cycles field if available, or count of quality_summary events per cycle as fallback)
  - First-Pass Acceptance Rate section shows percentage of work items accepted on first review pass per cycle
  - Script handles empty or missing metrics file with informative message
  - Script supports --help flag
module: null
domain: null
notes: |
  # WI-215: Cycle Reporting Script

  ## Design

  `scripts/report-cycle.sh` — focused on cycle-over-cycle quality trends.

  Uses the same bash-wrapping-python pattern as `scripts/report.sh`. Reuse the same auto-discovery logic and helper functions (fmt_tokens, fmt_ms).

  ### Sections:

  1. **Cycle-over-Cycle Quality Trends** — Same as report.sh section_quality_trends but as the primary output. Per-cycle finding counts by severity with trend indicator.

  2. **Convergence Speed** — Read convergence_cycles from metrics entries with event_type "convergence_summary" or "cycle_complete". If not available, count distinct quality_summary events per cycle as a proxy.

  3. **First-Pass Acceptance Rate** — For each cycle, count work items where first_pass_accepted is true vs total. Display as percentage. If first_pass_accepted field is not present in any entry, display "No first-pass data recorded" message.

  ### Auto-discovery
  Same pattern as report.sh: walk up from CWD looking for .ideate.json, read artifactDir, append /metrics.jsonl.
cycle_created: 1
cycle_modified: null
content_hash: 06bfb65b55a77ec3641d2ec55039f694b77bc4ad575f813504bf702708cbf50d
token_count: 581
file_path: .ideate/work-items/WI-215.yaml
resolution: "Verified complete: scripts/report-cycle.sh exists, documented in README"
