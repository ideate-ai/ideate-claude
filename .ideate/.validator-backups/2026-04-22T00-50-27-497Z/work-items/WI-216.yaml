id: WI-216
type: work_item
title: Cost reporting script
status: done
complexity: small
scope:
  - path: scripts/report-cost.sh
    op: create
depends: []
blocks: []
criteria:
  - scripts/report-cost.sh is executable (chmod +x)
  - Script requires python3 (checks and exits with error if not found)
  - Script accepts optional METRICS_FILE argument (same auto-discovery pattern as scripts/report.sh)
  - "Script produces markdown output with sections: Per-Work-Item Token Cost, Per-Cycle Token Cost, Cost Trends"
  - Per-Work-Item section shows total tokens (input + output) per work item, sorted by cost descending
  - Per-Cycle section shows total tokens per cycle with breakdown by phase
  - Cost Trends section shows cycle-over-cycle token usage with trend indicator
  - If cycle_total_cost_estimate is available in metrics entries, script includes dollar estimates
  - Script handles empty or missing metrics file with informative message
  - Script supports --help flag
module: null
domain: null
notes: |
  # WI-216: Cost Reporting Script

  ## Design

  `scripts/report-cost.sh` — focused on token and cost analysis.

  ### Sections:

  1. **Per-Work-Item Token Cost** — Group entries by work_item field. Sum input_tokens + output_tokens per item. Sort descending by total. Table: Work Item | Total Tokens | Input | Output | Cache Read | Spawns.

  2. **Per-Cycle Token Cost** — Group by cycle. Sum tokens per cycle with phase breakdown. Table: Cycle | Total Tokens | Plan | Execute | Review | Refine.

  3. **Cost Trends** — Cycle-over-cycle total token comparison with trend indicator. If cycle_total_cost_estimate is available, include dollar amounts.

  ### Cost estimation
  If entries have cycle_total_cost_estimate fields, use those directly. Otherwise, provide token counts only without dollar estimates. Do not hardcode token pricing — that changes frequently.
cycle_created: 1
cycle_modified: null
content_hash: 05735b364bb4f41b57f6da467f35c1b4e95ca290e89681068ff09cc2e69833fb
token_count: 499
file_path: .ideate/work-items/WI-216.yaml
resolution: "Verified complete: scripts/report-cost.sh exists, documented in README"
